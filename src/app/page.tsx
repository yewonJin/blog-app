export default function Home() {
  return (
    <div className="p-6">
      <h2 className="text-2xl font-semibold">Tailwindcss</h2>
      <p className="text-primaryText">
        TailwindCSS로 색상 시스템을 만들 때, 다양한 상황에서 재사용 가능한 색상
        이름을 정하는 것이 중요합니다. 아래와 같은 시스템을 추천합니다:
      </p>
      <p className="text-secondaryText">
        TailwindCSS로 색상 시스템을 만들 때, 다양한 상황에서 재사용 가능한 색상
        이름을 정하는 것이 중요합니다. 아래와 같은 시스템을 추천합니다:
      </p>
      <p className="text-tertiaryText">
        TailwindCSS로 색상 시스템을 만들 때, 다양한 상황에서 재사용 가능한 색상
        이름을 정하는 것이 중요합니다. 아래와 같은 시스템을 추천합니다:
      </p>
    </div>
  );
}
